---
name: article-engine
description: >
  Generates blog articles, pages, and content. Triggers on: "article", "blog post",
  "generate a page", "write a page", "create a page", "generate an article",
  "write an article", "create an article", "new article", "article engine",
  "article-engine", "write about", "create content about", "blog about",
  "generate content", "write a blog", "make an article", "make a page",
  "edit section", "update section", "configure gemini", "setup gemini",
  "reconfigure article engine". Also triggers on any topic with intent to produce
  article/page content (e.g. "Manchester United", "write about AI in logistics").
---

# Autonomous Article Engine v4.6

A portable, project-agnostic article generation engine. Generates complete standalone HTML article pages from minimal input by automatically adapting to the current project's design system, detecting reusable shells and components, and producing publication-ready content with research, images, TOC, trust elements, and section-level editing capabilities.

Works on any website: company sites, blogs, editorial platforms, portfolios, agency sites, educational pages, or custom projects.

## CRITICAL RULES

### Domain Integrity
> **Single authority:** `config/domain-integrity.md` — all agents reference that file.

**The topic determines the article domain. The website determines the presentation.**
- "Manchester United" on a logistics site → football article, logistics site's visual style
- "Quantum Computing" on a bakery blog → physics article, bakery blog's visual style
- NEVER force a topic into the website's industry
- The website is the PUBLISHER, not the SUBJECT

### Project Adaptation
**The skill always works against the CURRENT ACTIVE PROJECT.**
- Detect the project's shell, layout, typography, colors, and components automatically
- Never assume a specific brand, site structure, or design system
- Adapt to whatever the current project provides

### User-Facing Communication
**NEVER expose backend implementation details to end users.**
- NEVER mention "Supabase", "anon key", "service_role", "RLS", database tables, or config files
- NEVER tell users to configure environment variables (`SUPABASE_URL`, `SUPABASE_ANON_KEY`, etc.)
- The auth system is PRE-CONFIGURED and ships ready to use — users just sign up and log in
- If auth errors occur, say "Authentication service unavailable" — not "Supabase not configured"
- The bridge server starts with `node bridge/server.js` — no setup needed from the user
- Users interact with the system through: sign up → admin approves → edit access granted
- All backend config (`.supabase.json`, anon key) is embedded in the plugin and auto-restores if missing

---

## INPUT FORMATS ACCEPTED

- Single keyword: "Manchester United"
- Multiple keywords: "electric vehicles, battery tech"
- Phrase: "the history of chess in the Middle East"
- Sentence: "Write about how AI is changing modern football scouting"
- File reference: "use topics.txt" or "read my notes from notes.md"
- With component override: "use custom-components.html as the components file"
- With shell hint: "use about.html as the shell reference"
- **Section edit prompt:** structured prompt targeting a specific section for update (see Section Edit Mode below)

---

## OPERATION MODES

### Mode 1: Full Article Generation (default)
Runs the complete 20-step pipeline to produce a new article.

### Mode 2: Section Edit
Triggered when input matches a section edit prompt pattern:
- Contains "update section" or references a section ID
- Contains a structured edit prompt generated by the edit UI
- Runs a focused subset of the pipeline targeting only the specified section

**Section Edit Detection:**
If the input contains `SECTION_EDIT:` or references a `data-section-id`, enter Section Edit Mode (see Step 20).

---

## PIPELINE OVERVIEW

```
First-Time Setup Gate → Input → Domain Lock → Detect Shell → Detect Components →
Select Adaptation Mode → Analyze Presentation → Extract Writing Style → Research →
Generate Ideas → User Picks → Architecture + TOC → Image Plan →
Gemini Capability Check → Generate 4-6 Images → Image Placement → Sidebar TOC →
Write Article → Attach Section Metadata → Integrate Section Edit UI →
Build Edit Prompt Logic → Visual Trust Moderation → Final Consistency Pass → Deliver HTML
```

Steps 0-22, grouped into agent dispatches. Step 0 (First-Time Setup Gate) runs once per project. Step 0.1 (Claude CLI Check) runs EVERY time. Step 0.5 (Quota Check) runs every time. Step 13 (Gemini Capability Check) is for portability.

### Progress Messages

At each major phase, output a short progress line so the user knows what's happening. This is important because the full pipeline takes 12-20 minutes and users may think it's stuck without feedback.

Show these at the start of each phase:
- Step 1-2: `📋 Parsing topic and classifying domain...`
- Steps 3-8: `🔍 Analyzing your website's design system...`
- Step 9: `🔬 Researching topic — this may take a few minutes...`
- Step 10: `💡 Generating article concepts...`
- Step 11: `🏗️ Building article architecture...`
- Steps 12-15: `🖼️ Planning and generating images...`
- Step 16: `📑 Building table of contents...`
- Steps 17-22: `✍️ Writing the full article — this is the longest step...`
- After delivery: `✅ Article delivered!`

---

## INTERNAL MODULES

### Existing Modules
- First-Time Setup Gate
- Claude CLI Check (every run)
- Input Normalizer
- Topic Domain Classifier
- Current Project Shell Detector
- Current Project Component Detector
- Adaptation Mode Selector
- Website Presentation Analyzer
- Component Mapper
- Writing Style Extractor
- Research Orchestrator
- Idea Generator
- Article Architect
- Gemini Image Generator
- Table of Contents Builder
- Draft Writer
- Visual Trust Moderator
- Consistency Editor

### New Modules (v4)
- **Image Plan Builder** — strategically plans 4-6 images based on topic, angle, and section structure
- **Multi-Image Generator** — generates 4-6 varied, purposeful images via Gemini
- **Editable Section Mapper** — assigns stable IDs, types, roles, and metadata to each section
- **Section Edit UI Builder** — injects lightweight edit controls into the generated page
- **Section Edit Prompt Builder** — generates structured edit prompts from user section-edit requests
- **Section Update Orchestrator** — handles intelligent section rewrites using the same skill

### Gemini Portability Modules (v4.2)
- **Gemini Tool Resolver** — resolves the active image-generation capability at runtime through a multi-strategy approach: checks known tool names, aliases, config overrides, and mapped providers. Exposes ONE normalized internal interface for the rest of the skill. The rest of the skill calls the resolver, never raw tool names.
- **Gemini Alias Matcher** — maintains a list of known compatible Gemini image tool names/aliases and matches any available tool against them. Supports user-configured custom aliases via engine-config.md.
- **Gemini Capability Detector** — runs the layered detection strategy: skill config override → alias matching against available tools → project-local .mcp.json → user-global config → graceful fallback. Reports which detection path succeeded.
- **Gemini Config Loader** — loads Gemini settings from skill config, environment variables, project-local .mcp.json, or user-global MCP configs
- **Gemini Runtime Validator** — validates that the resolved Gemini tool actually works before generating images (test call or capability probe)
- **Portable Image Generation Orchestrator** — manages image generation through the normalized interface (never calls raw tool names). Copies images from MCP output to project `images/` folder using dynamic path parsing.
- **Image Generation Fallback Handler** — handles graceful degradation when ALL resolution strategies fail (placeholder HTML + exported prompts). Only activates after the full resolver chain has been exhausted.

### Structural Component Memory Modules (v4.3)
- **Structural Component Registry** — internal registry of 190 normalized content blueprints (plus shell, hero, prose structural blueprints) extracted from article-components.html. Stores STRUCTURE ONLY (layout, slots, hierarchy, relationships) — no brand colors, fonts, or visual identity. Located at `config/structural-component-registry.md`. Agents MUST read this file dynamically — never rely on a hardcoded blueprint list.
- **Article Shell Registry** — internal blueprint for the article page shell structure (two-column layout, hero placement, sidebar, TOC). Stored within the structural component registry.
- **Blueprint-to-Project Mapper** — maps internal structural blueprints to the active project's visual language at runtime. Takes a blueprint ID + project design tokens → outputs styled HTML.
- **Runtime Visual Adapter** — applies the active project's design tokens (colors, fonts, spacing, radii, shadows) on top of structural blueprints. Never uses frozen brand values from the source project.
- **Topic-Aware Composition Selector** — selects the optimal component mix based on topic type (data-heavy, how-to, narrative, comparison, etc.) using the composition guide in the registry.

---

## STRUCTURAL COMPONENT MEMORY

The skill carries an **internal structural component registry** (`config/structural-component-registry.md`) containing **190 content blueprints** plus 3 structural blueprints (article shell, hero, prose).

### Key Properties
- **Structure only** — no brand colors, fonts, or visual identity stored
- **Portable** — works in any project without external files
- **Runtime-adapted** — visual styling applied from the active project's design tokens
- **Topic-aware** — component composition varies by topic type
- **190 content blueprints** — organized by category (data-visualization, media, sequential-content, comparison, editorial, interactive, actionable-content, summary, typography, navigation, trust, feature-display, conversion, content-layout, chronological-content, tabular-data, process-flow, emphasis-content, feature-showcase, visual-content, social-proof, data-display, interactive-content, relationship-visualization, navigation-utility)

### Source
One-time ingestion from `article-components.html`. The registry is now self-contained within the skill. The original file is NOT required at runtime.

### Blueprint Discovery
The registry file is the **single source of truth** for available blueprints. Agents MUST read `config/structural-component-registry.md` dynamically to discover all blueprint IDs — never rely on a hardcoded list. The registry may be updated with new blueprints at any time.

Structural (auto-used): `bp-article-shell`, `bp-hero`, `bp-article-prose`
Content: 190 blueprints from BP-01 through BP-190 — read the registry file for the full list.

### Runtime Flow
1. Article Architect reads the structural registry and selects blueprints for each section
2. Draft Writer reads the selected blueprints and generates HTML structure from them
3. Runtime Visual Adapter applies the active project's design tokens to the generated HTML
4. Result: article uses proven structural patterns with project-appropriate visual styling

---

## STEPS 0, 0.1, 0.5 — PREFLIGHT CHECKS (dispatched to agent)

**Dispatch to `agents/preflight-check.md`** — this agent runs three checks:
1. **Step 0 (Setup Gate):** Gemini MCP configuration (runs once per project)
2. **Step 0.1 (CLI Check):** Claude CLI availability (runs every time)
3. **Step 0.5 (Quota Check):** Daily generation quota enforcement (runs every time)

The agent returns a preflight status report with `gemini_configured`, `cli_available`, and `quota_ok` flags. If `quota_ok` is false (limit exceeded, no auth), **stop — do not proceed with article generation.**

If status is `ready`, proceed to Step 1.

---

## STEP 1 — INPUT NORMALIZATION (inline)

Parse the user's input and normalize it into a structured topic seed.

**Process:**
1. Identify the input type (keyword / keywords / phrase / sentence / file / notes / section-edit)
2. If file reference: read the file and extract topics
3. If section edit prompt: enter Section Edit Mode (skip to Step 20)
4. Extract the core topic
5. Infer the intent (inform / guide / compare / analyze / explain / narrate / opinion)
6. Generate 3-5 possible subtopics
7. Check for ambiguity — if the topic is too broad or unclear, ask ONE clarifying question

**Check for overrides:**
- Component file override: "use X as the components file" → capture path
- Shell hint: "use X as the shell" → capture path
- Otherwise: auto-detect everything

**Output to user:**

```
TOPIC SEED
━━━━━━━━━━━━━━━━━━━━━━━━━
Topic: [normalized topic]
Intent: [inform/guide/compare/analyze/explain/narrate/opinion]
Subtopics: [list of 3-5]
Component file: [path or "auto-detect"]
Shell hint: [path or "auto-detect"]
Ambiguities: [any concerns, or "None — proceeding"]
━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## STEP 2 — DOMAIN CLASSIFICATION (inline)

Classify the topic's true domain BEFORE any project analysis.

**Process:**
1. Determine the topic's natural domain (sports, technology, science, business, etc.)
2. Determine the subdomain (football, AI/ML, archaeology, etc.)
3. Set the `domain_lock` — this CANNOT be changed by any downstream agent
4. Define `domain_keywords` — 10-15 terms that belong to this domain
5. Define `domain_drift_signals` — phrases that would indicate drift toward the website's industry

**Output (append to topic seed):**

```
DOMAIN LOCK
━━━━━━━━━━━━━━━━━━━━━━━━━
Domain: [primary domain]
Subdomain: [specific area]
Domain keywords: [list of 10-15]
Drift signals: [list of 5-10]
Lock status: LOCKED
━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## STEPS 3-8 — PROJECT INTELLIGENCE (agent dispatch)

Dispatch the `project-analyzer` agent. This single agent handles 6 substeps:
- Step 3: Shell Detection
- Step 4: Component Detection
- Step 5: Adaptation Mode Selection
- Step 6: Fallback Planning (if needed)
- Step 7: Presentation Analysis
- Step 8: Writing Style Extraction

**Prompt template for the agent:**

```
Analyze the current project for article generation. This is a PROJECT-AGNOSTIC analysis.
Do NOT assume any specific brand, site, or design system.

Topic: {topic_seed}
Domain Lock: {domain_lock}
Component file override: {component_file_path or "auto-detect"}
Shell hint: {shell_hint or "auto-detect"}
Project root: {current_working_directory}

CRITICAL: Your analysis determines HOW to present the article visually.
It does NOT influence WHAT the article is about. The topic domain is locked.

IMPORTANT: The skill carries an INTERNAL STRUCTURAL COMPONENT REGISTRY at
config/structural-component-registry.md with 20+ proven component blueprints.
These provide the structural source of truth. Your job is to detect the
project's VISUAL LANGUAGE (colors, fonts, spacing, shell) — not to find components.
The internal registry already has the component structures.

Run all 6 substeps:
1. SHELL DETECTION — find the current project's page shell (header, footer, wrapper, nav)
2. COMPONENT DETECTION — check if project has its own article components (override).
   If none found, the internal structural registry will be used (this is the default).
3. ADAPTATION MODE — select: existing (project has own components) / registry (use internal blueprints + project visual style) / fallback (no project shell either)
4. FALLBACK PLAN — if mode is registry or fallback, describe what visual adaptation is needed
5. PRESENTATION ANALYSIS — analyze the website's visual style (colors, fonts, spacing, radii, shadows)
6. WRITING STYLE EXTRACTION — extract writing patterns from samples

Return the PROJECT INTELLIGENCE REPORT.
```

**After agent returns:**
- Store the full PROJECT INTELLIGENCE REPORT
- Show status: "Project analyzed: {adaptation_mode} mode, {component_count} components, shell {detected/generated}"

---

## STEP 9 — RESEARCH (agent dispatch)

Dispatch the `research-engine` agent.

**Prompt template:**

```
Research this topic for article generation.

Topic: {topic_seed}
Domain Lock: {domain_lock}

CRITICAL DOMAIN RULE:
The topic domain is "{domain}" (subdomain: "{subdomain}").
ALL research must stay within this domain. Do NOT research connections to the
website's industry ({website_industry}). Research the topic AS IT IS.

Domain keywords: {domain_keywords}
Drift signals to AVOID: {domain_drift_signals}

Research tool priority:
1. Gemini MCP — try first (detect at runtime, do not assume availability)
2. WebSearch + WebFetch — supplement, verify, and fallback if Gemini unavailable
3. Context7 — only if topic involves a specific library/framework

Run all 6 research rounds and return the RESEARCH REPORT.
Also generate 4-6 IMAGE PROMPTS for Gemini image generation (see image planning rules).
```

**After agent returns:**
- Store RESEARCH REPORT and IMAGE PROMPTS
- Show status: "Research complete: {fact_count} facts, {angle_count} fresh angles"

---

## STEP 9B — BLUEPRINT HISTORY CHECK (automatic)

Before dispatching the architect, read the blueprint history file to know which blueprints were used in recent articles. This ensures every new article gets structurally different components.

**How it works:** The pipeline maintains a lightweight history file at `config/.blueprint-history.json` that tracks blueprints used in the last 5 articles. The file is local state (gitignored) — each user builds their own history as they generate articles.

**Process:**

1. Read `config/.blueprint-history.json` from the plugin directory
2. If the file doesn't exist or is empty (`"history": []`), create it — this is the first article, all 190 blueprints are available
3. Extract all blueprint IDs from the history entries and combine into one exclusion list
4. The history keeps only the last 5 articles. If there are more than 5 entries, ignore the oldest ones

**Output:**
```
RECENT BLUEPRINT HISTORY:
- [article-file-1]: bp-stats-cards, bp-timeline, bp-pull-quote (8 blueprints)
- [article-file-2]: bp-feature-grid, bp-checklist, bp-faq-accordion (9 blueprints)
Excluded (recent): [combined unique list]
Available: [190 minus excluded count] of 190
```

If history is empty:
```
RECENT BLUEPRINT HISTORY: none (first article — all 190 blueprints available)
Available: 190 of 190
```

Store the exclusion list for the architect dispatch.

**IMPORTANT:** After the article is delivered (end of Step 22), the pipeline MUST append the new article's blueprints to the history file. See "Post-Delivery: Update Blueprint History" below Step 22.

---

## STEP 10 — ARTICLE CONCEPTS (agent dispatch + user selection)

Dispatch the `article-architect` agent (Phase 1).

**Prompt template:**

```
Phase 1: Generate 5 article concepts.

Topic: {topic_seed}
Domain Lock: {domain_lock}
Adaptation Mode: {adaptation_mode}

CRITICAL: All concepts must be pure {domain} articles. Do NOT bridge to {website_industry}.
Score "presentation fit" (component compatibility), NOT "industry fit".

Research summary:
{paste RESEARCH REPORT}

Available components (structural types):
{paste COMPONENT INVENTORY from project intelligence report}

Generate 5 genuinely different concepts.
```

**After agent returns:**
Present concepts to user:

```
ARTICLE CONCEPTS
━━━━━━━━━━━━━━━━━━━━━━━━━

1. [Title]
   Angle: [editorial angle]
   Value: [reader value]
   Fit: ★★★★☆ (presentation compatibility)
   Sections: ~[N] | Words: ~[N]

[through 5]

━━━━━━━━━━━━━━━━━━━━━━━━━
Pick a number (1-5), or say "regenerate".
━━━━━━━━━━━━━━━━━━━━━━━━━
```

**WAIT for user selection.**

---

## STEP 11 — ARTICLE ARCHITECTURE + TOC + SIDEBAR (resume agent)

Resume the `article-architect` agent for Phase 2.

**Resume prompt:**

```
Phase 2: Build the full article architecture.

Selected concept: #{user_selection_number}
Domain Lock: {domain_lock}
Adaptation Mode: {adaptation_mode}

Component inventory:
{paste COMPONENT INVENTORY — include classification_map}

BLUEPRINT SELECTION — RANDOM WITH EXCLUSION:
{paste exclusion list from Step 9B}
RULES:
- Read the structural component registry (config/structural-component-registry.md) for all available blueprints
- EXCLUDE the blueprints in the exclusion list above (used in last 5 articles)
- From the remaining available pool, RANDOMLY select 8-10 blueprints that fit the topic
  - 8 for narrative/editorial topics
  - 9-10 for data-heavy/analytical topics
- Selection must be RANDOM — do not always pick the same "safe" blueprints
- Each blueprint must genuinely fit a section's content purpose (no forcing)

REQUIREMENTS:
1. Section sequence with 8-10 component types — RANDOMLY selected from available pool (excluding recent history)
2. TABLE OF CONTENTS with sidebar labels (skip hero, max ~40 chars per label)
3. TRUST LAYER plan (minimum 4 elements)
4. Image placement plan (4-6 sections marked for images)
5. Two-column layout: hero full-width, content + sidebar TOC in grid
6. SECTION METADATA for each section (id, type, role, content purpose) for edit system
```

**After agent returns:**
- Store ARTICLE ARCHITECTURE
- Show status: "Architecture ready: {section_count} sections, {component_count} components, sidebar TOC with {heading_count} entries"

---

## STEPS 12-15 — IMAGE PLANNING & GENERATION (dispatched to agent)

**Dispatch to `agents/image-generator.md`** — this agent handles:
1. **Step 12 (Image Plan):** Strategic planning of 4-6 images per article
2. **Step 13 (Capability Check):** Portable Gemini tool resolution (7-strategy chain)
3. **Step 14 (Generation):** Image generation via Gemini or graceful fallback (placeholders + prompt export)
4. **Step 15 (Placement):** Image-to-section mapping and fit verification

The agent returns a generated images report and image placement map. If Gemini is unavailable, fallback mode produces placeholder HTML with exportable prompts.

---

## STEP 16 — SIDEBAR TABLE OF CONTENTS GENERATION (inline)

Generate the TOC structure from the finalized architecture.

This is prepared here and passed to the draft writer for rendering. Both sidebar and inline TOC use the same heading list.

---

## STEPS 17-22 — WRITING + SECTION EDITING + DELIVERY (agent dispatch)

These steps are handled as a single agent dispatch to the `draft-writer` agent:

- **Step 17** — Write the full HTML article (structure, content, components, images, styles)
- **Step 18** — Attach section metadata (`data-section-id`, `data-section-type`, etc.) to each content section
- **Step 19** — Integrate the section edit UI (overlay HTML, edit triggers, auth-aware login form)
- **Step 20** — Build the edit prompt generation JS (SECTION_EDIT builder, bridge server integration, fallback)
- **Step 21** — Visual trust moderation (verify all trust elements, check for banned patterns)
- **Step 22** — Final consistency pass + delivery (validate HTML, write file, generate delivery report)

Dispatch the `draft-writer` agent.

**Prompt template:**

```
Write the full article and output a complete HTML file with section editing capabilities.

DOMAIN LOCK: {domain_lock}
ADAPTATION MODE: {adaptation_mode}

ARTICLE ARCHITECTURE:
{paste full architecture — TOC, trust layer, image slots, SECTION METADATA}

SHELL STRATEGY:
{paste the SHELL STRATEGY section from project intelligence report}
If adaptation mode is "existing": read the detected shell file and use it as the page wrapper.
If adaptation mode is "adapted" or "fallback": read the fallback shell template from the plugin
  directory at config/article-shell-template.html and customize it based on the detected design tokens.

DESIGN TOKENS:
{paste the DESIGN TOKENS section from project intelligence report — colors, fonts, spacing, etc.}

WRITING STYLE MODEL:
{paste WRITING STYLE MODEL}

EVIDENCE BANK:
{paste EVIDENCE BANK}

GENERATED IMAGES:
{paste image filenames, types, and section assignments from IMAGE PLACEMENT MAP}
Image generation mode: {gemini / fallback}
If fallback: images are placeholder comments — include exported prompts in delivery report

TABLE OF CONTENTS (with sidebar labels):
{paste TOC structure}

TRUST LAYER PLAN:
{paste trust layer plan}

COMPONENT SOURCE:
- Adaptation mode: {mode}
- If "existing": read components from {component_file_path}
- If "registry" (default): read structural blueprints from config/structural-component-registry.md.
  Each section's blueprint ID (bp-XXX) is in the architecture. Generate HTML from the blueprint's
  structural pattern and slot definitions, then apply the project's design tokens for visual styling.
  NEVER use Axiom-specific colors/fonts — only the active project's detected tokens or fallback defaults.
- If "fallback": same as registry mode for components, plus use fallback shell + fallback design tokens

CONFIGURATION:
- Plugin directory: .claude/plugins/article-engine/
- Banned patterns: config/banned-patterns.md
- Structural component registry: config/structural-component-registry.md
- Fallback shell template: config/article-shell-template.html
- Output filename: article-{slug}.html (write to project root or configured output folder)
- Image paths: use "images/" prefix (relative, no leading slash)

LAYOUT:
- Shell template uses two-column layout: article main + sticky sidebar TOC
- Hero → {{ARTICLE_HERO}} (full width, above grid)
- Inline mobile TOC → {{ARTICLE_TOC_INLINE}}
- Article sections 2+ → {{ARTICLE_CONTENT}}
- Sidebar TOC → {{ARTICLE_TOC_SIDEBAR}}
- Build BOTH sidebar and inline TOC from same heading list

SECTION EDITING REQUIREMENTS:
- Assign stable section metadata (id, type, role, purpose) to every editable section
- Inject the section edit UI (hover controls, edit input overlay) into the page
- Include the edit system JavaScript (bridge server → clipboard fallback)
- Each section must have data attributes: data-section-id, data-section-type, data-section-role
- The edit UI must be professional, unobtrusive, and match the page design
- Include CSS for the section edit system (including status toast and fallback toggle styles)
- Include JS for the edit system: buildEditPrompt(), showEditStatus(), bridge server fetch with clipboard fallback
- The "Apply Edit" button must try http://127.0.0.1:19847/apply-edit first (bridge server), then fall back to clipboard auto-copy + /apply-edit command

Write all sections, build TOC, insert images (4-6), apply trust layer, attach section metadata,
integrate edit UI, build edit system logic, assemble into shell, run consistency pass
(domain drift + sidebar TOC validation + section ID stability + image quality check),
write final HTML. Return ARTICLE DELIVERY REPORT.
```

**After agent returns — auto-start bridge server:**

After the draft-writer agent completes and the article is delivered, automatically start the bridge server for live section editing. This is MANDATORY — do not skip this step.

0. **Check Node.js is available:** Run `node --version` via Bash. If Node.js is not installed, inform the user:
   ```
   ⚠ Node.js not found — bridge server requires Node.js 18+.
     Install from: https://nodejs.org
     Section editing will use clipboard fallback until Node.js is available.
   ```
   Skip steps 1-3 and proceed to step 4 with bridge status "not started (Node.js missing)".

1. **Check if bridge is already running:** Use the Bash tool to run:
   ```
   curl -s http://127.0.0.1:19847/health
   ```
   If the response contains `"status":"ok"`, the bridge is already running — skip to step 4.

2. **Start the bridge server:** Use the Bash tool with `run_in_background: true` and `timeout: 600000`:
   ```
   node "<plugin-dir>/bridge/server.js" "<project-dir>"
   ```
   Replace `<plugin-dir>` with the actual path to the article-engine plugin directory (find it via `bridge/server.js` glob).
   Replace `<project-dir>` with the user's working directory.

3. **Verify it started:** Use the Bash tool to run:
   ```
   curl -s http://127.0.0.1:19847/health
   ```
   If the response contains `"status":"ok"`, the bridge started successfully.
   If it fails, retry once after 2 seconds. If still failing, continue — the edit system will fall back to clipboard + `/apply-edit` command.

4. **Include bridge status in the delivery report** (running / failed to start).

```
ARTICLE DELIVERED
━━━━━━━━━━━━━━━━━━━━━━━━━
File: {filename}
Title: {title}
Domain: {domain} (locked — no drift)
Adaptation: {mode} ({shell_source})
Words: ~{word_count}
Sections: {section_count}
Layout: two-column (article + sticky sidebar TOC)
Components: {component_count} unique types
Images: {image_count} ({generation_mode})
Sidebar TOC: {entry_count} entries
Trust elements: {list}
Editable sections: {count} with edit UI
Edit system: bridge server + auto-clipboard + /apply-edit fallback
Consistency: {all passed / flags}

GEMINI INTEGRATION DIAGNOSTIC
━━━━━━━━━━━━━━━━━━━━━━━━━
Resolution steps attempted:
  1. Config override: {checked/skipped} — {result}
  2. Known alias scan: {checked} — {matched: tool_name / no match}
  3. Custom alias scan: {checked/skipped} — {result}
  4. Pattern match: {checked} — {matched: tool_name / no match}
  5. Project .mcp.json: {found/not found} — {gemini: yes/no}
  6. Global config: {found/not found} — {gemini: yes/no}

Resolved tool: {resolved_image_tool or "none"}
Detection method: {config-override / known-alias / custom-alias / pattern-match / project-mcp / global-mcp / none}
Tool available: {yes / no}
Image generation: {succeeded / failed / skipped / fallback}
Images generated: {N of M planned}
Fallback used: {yes — placeholder+prompts / no}
Failure reason: {none / no compatible tool found / generation error / ...}
━━━━━━━━━━━━━━━━━━━━━━━━━

BRIDGE SERVER
━━━━━━━━━━━━━━━━━━━━━━━━━
Status: {running / not started}
URL: http://127.0.0.1:19847
━━━━━━━━━━━━━━━━━━━━━━━━━

Next steps (optional):
- "preview" → open in browser
- "alternatives" → generate 3 title alternatives
- "edit section [id]" → edit a specific section directly
- hover over any section → click Edit → type changes → click Apply Edit → automatic update (if bridge running)
- /start-bridge → start the bridge server for fully automatic browser edits
- /stop-bridge → stop the bridge server
- /apply-edit → manually apply an edit from clipboard (fallback if bridge not running)
- "another article" → start pipeline with new topic
━━━━━━━━━━━━━━━━━━━━━━━━━
```

**Post-Delivery: Update Blueprint History**

After the delivery report is shown, update the blueprint history file so the next article avoids these blueprints:

1. Read `config/.blueprint-history.json` from the plugin directory
2. If the file doesn't exist, create it with `{ "history": [] }`
3. Append a new entry:
   ```json
   {
     "file": "{output_filename}",
     "date": "{YYYY-MM-DD}",
     "blueprints": ["{bp-id-1}", "{bp-id-2}", "..."]
   }
   ```
4. If the history array has more than 5 entries, remove the oldest ones (keep only the last 5)
5. Write the updated file back

This is automatic and silent — no output to the user.

---

## SECTION EDIT MODE

When the skill receives a section edit prompt (from the `/apply-edit` command, direct input, or clipboard), it operates in Section Edit Mode.

### Edit Flow (Primary — Bridge Server, fully automatic)

1. User opens the generated article in a browser
2. User hovers over a section → clicks the Edit button
3. User types their desired changes in the overlay textarea
4. User clicks "Apply Edit"
5. The browser sends the SECTION_EDIT prompt to the bridge server at `http://127.0.0.1:19847/apply-edit`
6. The bridge server spawns `claude -p` to process the edit automatically
7. The section is updated — user refreshes the page to see changes

**The bridge server is auto-started after article generation.** Users can also manually start/stop it with `/start-bridge` and `/stop-bridge`.

### Edit Flow (Fallback — Clipboard + Command)

If the bridge server is not running:

1. The "Apply Edit" button auto-copies the SECTION_EDIT prompt to clipboard
2. User switches to Claude Code and types `/apply-edit`
3. The `/apply-edit` command reads the clipboard and processes the edit

If clipboard also fails, the overlay shows the raw prompt for manual copy.

### Detection

Section Edit Mode activates when input contains:
- `SECTION_EDIT:` prefix (from `/apply-edit` command or direct paste)
- A `data-section-id` reference
- "update section [id]" pattern
- A structured edit prompt generated by the page's edit UI

### Section Edit Prompt Template

The page's edit UI generates prompts in this format:

```
SECTION_EDIT:
Use the autonomous-article-engine skill to update section [SECTION_ID].

Topic: [TOPIC]
Article file: [FILENAME]
Section ID: [SECTION_ID]
Section type: [SECTION_TYPE]
Section role: [SECTION_ROLE]
Section purpose: [SECTION_PURPOSE]
Current section heading: [HEADING]

User requested change: [USER_EDIT_REQUEST]

RULES:
- Update only this section unless a minimal surrounding adjustment is required for consistency
- Preserve topic domain integrity
- Preserve page style and component compatibility
- Improve the section intelligently and professionally, not just literally
- Keep the result aligned with the rest of the article
- If the edit affects a heading, update the sidebar TOC entry to match
- Maintain the section's data attributes (data-section-id, data-section-type, data-section-role)
```

### Section Edit Execution

When in Section Edit Mode:

1. **Parse the edit prompt** — extract section ID, type, role, file path, and user instruction
2. **Read the target HTML file** — load the full article page
3. **Locate the target section** — find the section by its `data-section-id`
4. **Understand context** — read surrounding sections to understand the section's role in the article
5. **Interpret the edit request** intelligently:
   - Understand the purpose of the section
   - Understand its role in the full article
   - Interpret the user request contextually (not literally)
   - Consider tone, clarity, structure, and usefulness
6. **Rewrite the section** — produce an improved version that:
   - Addresses the user's edit request
   - Maintains domain integrity
   - Preserves component structure and CSS classes
   - Keeps `data-section-id`, `data-section-type`, `data-section-role` attributes
   - Improves quality beyond the bare minimum
7. **Check scope** — determine if neighboring sections need minimal adjustments:
   - Heading consistency with TOC
   - Factual continuity
   - Visual structure continuity
   - If TOC heading changed, update both sidebar and inline TOC entries
8. **Apply the edit** — use the Edit tool to replace the old section HTML with the new version
9. **Report the edit:**

```
SECTION EDIT COMPLETE
━━━━━━━━━━━━━━━━━━━━━━━━━
File: [filename]
Section: [section_id] ([section_type])
Change requested: [user instruction summary]
Changes made: [description of what was changed]
Scope: [section-only / section + TOC update / section + minor neighbor adjustments]
Domain integrity: preserved
━━━━━━━━━━━━━━━━━━━━━━━━━
```

### Section Edit Intelligence Rules

The system must NOT do a weak literal rewrite. It must behave intelligently:
- Understand the purpose of the selected section
- Understand its role in the full article
- Interpret the user request contextually
- Improve clarity, structure, tone, and usefulness
- Preserve page consistency
- Keep the section aligned with the topic domain
- Keep design/component compatibility
- The update should feel like a smart editorial improvement

### Edit Scope Rules

By default, edits are section-local. Only modify surrounding sections if absolutely necessary for:
- Heading consistency
- Factual continuity
- Visual structure continuity
- TOC heading synchronization

If neighboring changes are needed, keep them minimal and explicit.

---

## ERROR HANDLING

- **Agent fails:** Show error, offer retry
- **Gemini MCP unavailable for research:** Degrade to WebSearch silently
- **Gemini image generation unavailable:** Run full resolver chain (known aliases → custom aliases → pattern match → config check) → only activate fallback after ALL resolution strategies fail → note in report with full diagnostic block showing each attempted step
- **Gemini image generation fails mid-run:** Retry once, then fall back to placeholder for that image. Continue with remaining images.
- **MCP output path not parseable:** Report error, skip that image, continue with others
- **Image copy to project folder fails:** Report path error, include MCP output path in report for manual copy
- **Image quality insufficient:** Regenerate weak/repetitive images (max 2 retries per image)
- **No component library found:** Switch to fallback mode automatically
- **No shell found:** Generate fallback shell automatically
- **No writing samples:** Infer style from website analysis
- **Domain drift detected:** Reject output, regenerate with stricter enforcement
- **Section edit target not found:** Report error with section ID, suggest available sections
- **User says "stop" or "cancel":** Stop immediately, report progress

## PERFORMANCE RULES

- Max 6 research rounds
- Exactly 5 article concepts
- Max 5 writing samples read
- 8-10 unique component types per article (8 for narrative topics, up to 10 for data-heavy topics)
- Min 4 generated images, max 6 (if Gemini available)
- Sidebar TOC required for 5+ section articles (sticky desktop + inline mobile)
- Sidebar TOC must match final headings exactly
- Two-column layout mandatory: hero full-width, content + sidebar in grid
- Trust layer elements required on every article
- Section edit UI required on every generated article (CSS + overlay HTML + JS + per-section triggers + auto-clipboard + /apply-edit command — see Validation U)
- Every section must have 5 data attributes: data-section-id, data-section-type, data-section-role, data-section-heading, data-section-purpose
- Section IDs must follow the pattern `section-{N}` where N is the section order (section-1, section-2, etc.) for consistency with TOC anchors and the edit system
- Edit prompt must include: skill name, article topic, article file, section ID, section type, section role, section purpose, section heading, user instruction
- Reuse project analysis for second article in same session

## VALIDATION RULES

### A. Section ID Stability Check
Section identifiers must remain stable enough for edit reuse. IDs follow the pattern `section-{N}` where N is the section order. IDs must not change between edits unless a section is added or removed.

### B. Section Edit Scope Check
An edit should only affect the intended section unless minimal nearby edits are required. The consistency pass must verify that no unintended sections were modified.

### C. Edit Prompt Quality Check
Generated edit prompts must be structured, reusable, and clear. They must contain all required fields (section ID, type, role, topic, user instruction).

### D. Multi-Image Quality Check
All 4-6 generated images must be relevant to the topic, non-repetitive, and visually useful. No two images should have the same composition or purpose.

### E. Image-Section Match Check
Each image must fit its assigned section's purpose. A hero image should be atmospheric/broad; a contextual image should support the section's specific content.

### F. Professional UI Check
Edit controls must feel clean, integrated, and professional. They must not break the page design, appear toy-like, or distract from reading.

### G. Gemini Portability Check
No hardcoded paths to Gemini MCP output directories. All image paths must be relative to the project root. The skill must never assume a specific MCP server location or project folder.

### H. Single-Tool-Name Dependency Check
The skill must NEVER depend on one exact Gemini tool name. It must use the Gemini Tool Resolver which checks known aliases, custom aliases, pattern matching, and config overrides. If any compatible Gemini image tool is available under any recognized name, the skill must find and use it.

### I. Fallback Threshold Check
Fallback mode must ONLY activate after the full resolver chain has been exhausted (config override → known aliases → custom aliases → pattern match → project config → global config). A single tool name being absent does NOT trigger fallback — all resolution paths must fail first.

### J. Normalized Interface Check
The rest of the skill (Step 14 image generation, draft-writer, delivery report) must use `resolved_image_tool` from the Gemini Tool Resolver output. No downstream step may directly reference a hardcoded Gemini tool name.

### K. Cross-Project Capability Check
The skill must work identically in any project directory. No references to specific project paths, brand names, or folder structures outside the current working directory. Gemini resolution must be independent from where the skill was authored.

### L. Alias Resolution Check
Alternative compatible Gemini tool names must be supported. If a user's Gemini MCP exposes the tool under a different name (e.g., `mcp__gemini__generate-image` vs `mcp__gemini__gemini-generate-image`), the skill must still detect and use it.

### M. Diagnostic Output Check
The delivery report must include a Gemini integration diagnostic block showing ALL resolution steps attempted, which step succeeded (or that all failed), the resolved tool name, detection method, and generation results.

### N. Image Path Portability Check
All image `src` attributes in generated HTML must use relative paths (`images/article-{slug}-{N}.png`). Never use absolute paths or MCP output directory paths.

### O. Structure Retention Check
The internal structural registry must preserve meaningful component structures. Generated articles must use recognizable blueprint patterns from the registry.

### P. Style Stripping Check
No Axiom-specific colors (#3F7537, #D4FB50, #19342C), fonts (Alexandria, Poppins), or brand styling may be frozen into generated articles. All visual values must come from the active project's design tokens or fallback defaults.

### Q. Runtime Independence Check
The skill must NOT require `article-components.html` to be present at runtime. The internal structural registry carries all needed component structures.

### R. Blueprint Quality Check
Stored blueprints must be reusable across topics and projects. No single-use or project-specific structures.

### S. Visual Adaptation Check
Generated HTML must adapt visually to the current project's design tokens. The same blueprint used in two different projects must produce visually different (but structurally identical) output.

### T. Cross-Project Structural Reuse Check
The skill must behave identically in any project. The structural registry travels with the plugin — no external file dependencies.

### U. Section Edit UI Implementation Check (CRITICAL)
**Every generated article MUST include ALL of these — no exceptions:**
1. **Section wrappers:** Every content section wrapped in `<section>` with `id`, `class="article-section"`, and ALL five data attributes: `data-section-id`, `data-section-type`, `data-section-role`, `data-section-heading`, `data-section-purpose`
2. **Edit trigger buttons:** Every `<section class="article-section">` must contain a `<button class="section-edit-trigger">` as its first child with `data-edit-section` pointing to the section ID
3. **Edit overlay HTML:** Exactly ONE `<div class="section-edit-overlay" id="section-edit-overlay">` before closing `</body>`, containing the edit panel with textarea input, Cancel/Apply Edit buttons, status display area, fallback toggle, and prompt result display with Copy button
4. **Edit system JS:** JavaScript that wires up trigger buttons → overlay → auto-clipboard copy → status toast → fallback prompt display. Must include `ARTICLE_TOPIC` and `ARTICLE_FILE` constants, `buildEditPrompt()` helper, and `showEditStatus()` helper. The "Apply Edit" button auto-copies the SECTION_EDIT prompt to clipboard and instructs user to type `/apply-edit` in Claude Code. Fallback shows raw prompt for manual copy if clipboard fails.
5. **Edit CSS:** Full section edit system styles (trigger button, overlay, panel, input, buttons, status toast with success/error variants, fallback toggle, prompt result, copy button, print hiding)
6. **Section IDs must be numeric:** Use `section-1`, `section-2`, `section-3` etc. — following the `section-{N}` pattern for consistency with TOC anchors and the edit system JS

**If any of these are missing, the article fails validation.** The draft-writer agent's Phases F, G, H define the exact implementation. The orchestrator must verify the output contains all edit system elements before delivering.

## DOMAIN DRIFT DETECTION

An output has drifted if ANY of these are true:
- Main subject shifted from {topic} to {website_industry}
- More than 20% discusses {website_industry} when topic is unrelated
- Title/headings reference {website_industry} without warrant
- Analogies forcefully map topic to {website_industry}

## ADAPTATION MODE REFERENCE

| Mode | Shell | Components | When |
|------|-------|------------|------|
| **Existing** | Project's own shell | Project's own components | Project has article pages + its own component library |
| **Registry** | Project's shell (detected) | Internal structural registry blueprints + project visual tokens | Project has a shell but no article components (DEFAULT for most projects) |
| **Fallback** | Generated neutral shell | Internal structural registry blueprints + fallback design tokens | New/empty project or no usable shell patterns |

**The internal structural registry is used in both Registry and Fallback modes.** The only difference is whether the project provides a shell and visual tokens (Registry) or the skill generates/defaults them (Fallback).

The registry contains 20+ proven component blueprints from `config/structural-component-registry.md`. These provide structure; the active project provides visual adaptation.
